// Re-export Theme type from store for backwards compatibility
// TODO: Update all imports to use $lib/stores/theme directly
// This file can be removed once all usages are migrated
export type { Theme } from '$lib/stores/theme';
